# 巡回の生成物。GitHub Actions のキャッシュで持ち回るのでコミット不要
out/
state.json
crawl.log

# OS / エディタ
.DS_Store
.vscode/
__pycache__/
*.pyc

# Cloudflare
.wrangler/

# サイトに置く巡回結果（deploy.sh が out/ からコピーする）
site/data.json
_to_delete/
old/

# 個別のやりとりの下書き。相手方の宛先や担当者名が入るため、
# このリポジトリは公開なので追跡しない（告知.md 側にも個人情報は書かない）
返信_*.md

# 秘密情報。2026-08-14に、SSHのログイン情報・X APIのキー・Telegramのトークンを
# 書いたメモ（untitled text.txt / untitled text 2.txt）と、楽天のセッションCookie
# （rakuten_session.json）を、このリポジトリへ誤って公開した。
# 拡張子のない下書きメモが混ざりやすいので、名前で丸ごと弾く。
untitled*
*.env
.env*
rakuten_session.json
*session*.json
*_secrets.*

# 流出への対応で作った手元の道具。サイトの動作には関係しない。
# コメントに事故の経緯（何が漏れたか）を具体的に書いてあるため、
# 公開リポジトリには置かない。手元には残してある。
purge-secrets.sh
scan-history.sh

# 出典リンクの確認記録（実行のたびに変わるので追跡しない）
links.json

# 私（Claude）がこのフォルダに置いた作業用のファイル。
# サイトの動作には関係しないので追跡しない。2026-09-04、刈り込みの記録を
# ここに置いたら publish.sh の未追跡チェックに引っかかった（正しい動作）。
Claude outputs/
